version: "3.9"

services:
  rag-pipeline:
    build: .
    container_name: rag_csv_pipeline
    ports:
      - "8000:8000"         
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./vector_store:/app/vector_store
